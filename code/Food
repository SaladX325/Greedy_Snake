#pragma once
#include <stdlib.h>
#include <time.h>
class food
{
public:
	int x, y;
  food(): x(0), y(0) {}
	void generate(int a, int b) {
		srand(time(0));
		x = rand() % a;
		y = rand() % b;
	}
	int fx() { return x; }
	int fy() { return y; }
};
